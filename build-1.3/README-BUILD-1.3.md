# Release 3.0 / Build 1.3 — Admin Model Control

Adds an Access-protected `/model-control.html` page and APIs for model runtime visibility, prediction counts, notes, and safe challenger enable/disable controls.

This build does not permit production promotion, role changes, or disabling the production model.
