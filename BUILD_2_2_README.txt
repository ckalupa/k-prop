MLB K-Prop Release 3.1 Build 2.2 - Pitcher Game Logs

Adds controlled MLB boxscore ingestion for pitcher appearances, immutable raw boxscore snapshots,
sync health/status APIs, an admin page, and hourly scheduled refresh at minute 10.

Admin page:
https://admin.mlb.kalupa.net/pitcher-log-sync.html

After deployment, click Sync now once for the initial two-day load.
