MLB K-Prop Release 3.6 - Build 7.2.1

Statcast Admin Styling Hotfix

- Fixes the Statcast admin page referencing missing /admin-common.css.
- Reuses the existing shared /styles.css + /lineup-sync.css admin styling used by the other sync pages.
- Keeps Build 7.2 Statcast ingestion logic unchanged.
- No schema migration.
- Production v13 and live v14 remain unchanged.
